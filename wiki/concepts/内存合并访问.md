# 内存合并访问

## 定义

当一个 warp 中多个线程对 global memory 的访问能落在同一批 burst 读取范围内时，形成更高效的合并访问模式。

## 它解决什么问题

- 降低 DRAM 访问开销
- 减少因访存模式离散而带来的带宽浪费

## 核心机制

- Warp 中线程按更连续、更对齐的地址模式访问内存
- 让一次 burst 尽可能服务更多线程的数据需求
- 在矩阵乘法和张量变换里，线程布局与数据布局要一起设计
- 一个常见的经验模型是：同一 warp 的 32 个线程若能把地址落进尽量少的 `128B` cache line / transaction，搬运利用率通常更高
- 一个实用判断法是把 `threadIdx.x = 0..31` 代入地址表达式，检查相邻线程地址差是否接近 `sizeof(T)` 且首地址对齐；若步长变成 `N * sizeof(T)`，往往说明访问开始发散
- 典型反模式包括 `A[threadIdx.x * N + threadIdx.y]` 这类跨行步进访问，以及 `AoS` 里只取单字段导致的跨结构体跨步读取；常见修复是换线程映射、重排布局或改成 `SoA`

## 关键权衡

- 连续对齐的访存通常更快
- 一旦维度、padding 或线程映射不合适，就可能破坏 coalescing
- 读路径和写路径经过的缓存层未必完全一致，但判断 coalescing 的核心仍是“事务数是否被压低”

## 相关实体

- [[../entities/Stanford CS336]]

## 相关来源

- [[../sources/斯坦福CS336 Lecture 5 - GPUs]]
- [[../sources/你一定要知道：CUDA优化六要]]
- [[../sources/CUDA优化维度框架]]
- [[../sources/CUDA内存层次与动态共享内存问答整理]]

## 相关概念

- [[GPU执行模型]]
- [[CUDA内存层次]]
- [[Tiling]]

## 研究备注

- 后续可补 row-major / column-major 布局在不同线程映射下的 coalescing 对比图
- `Nsight Compute` 里一个直观信号是 `sectors / requests` 是否明显高于理想值；若远高于理论下界，常意味着 warp 访问已开始离散
