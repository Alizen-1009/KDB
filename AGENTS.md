# AI Infra LLM Wiki Schema

本文件是这个知识库的 schema。任何 LLM 在维护本仓库前，都应先阅读本文件，再阅读 `wiki/index.md` 与 `wiki/log.md`。

这个仓库默认作为 Obsidian Vault 使用，因此生成内容时要优先考虑 Obsidian 的阅读体验和双向链接体验。

## 目标

把 AI infra 方向的原始资料持续编译为一个可增长、可追溯、可查询的 Markdown wiki，而不是一次性问答记录。

## 三层结构

### 1. Raw

`raw/` 是原始资料层，只读不写。

- `raw/articles/`：博客、公众号文章、技术评论、设计说明
- `raw/papers/`：论文、技术报告、公开文档
- `raw/repos/`：项目源码、README、设计文档
- `raw/datasets/`：评测集、训练数据说明、benchmark 结果
- `raw/images/`：架构图、性能图、截图、表格图片
- `raw/code/`：实验脚本、配置片段、内核代码片段

原则：

- 不改写原始资料内容
- 不删除原始资料中的事实痕迹
- 如果原始资料有冲突，在 wiki 层标注，而不是在 raw 层修正

### 2. Wiki

`wiki/` 是编译后的知识层，由 LLM 维护。

- `wiki/sources/`：单份来源摘要
- `wiki/entities/`：人物、项目、框架、组织、硬件、数据集、benchmark 等实体
- `wiki/concepts/`：概念、机制、算法、术语
- `wiki/maps/`：主题地图（每个 topic 一页，顶部是手写导读，下半是自动清单）
- `wiki/index.md`：总索引
- `wiki/log.md`：操作日志

原则：

- 优先写结构化 Markdown，而不是长篇散文
- 所有重要结论尽量追溯到 `raw/` 中的来源
- 概念页尽量链接到来源页和实体页
- 新资料进入后，优先更新现有页面，再决定是否新增页面

### 3. Schema

本文件就是 schema，定义命名约定、工作流、维护标准。

## 命名约定

- 文件名优先使用中文短标题，必要时补年份、项目名或作者
- 单篇来源摘要文件名尽量与原始资料同名；但论文用论文标题，不用 arXiv 编号
- 概念页使用“概念名.md”
- 实体页使用“实体名.md”

## Frontmatter 规范

`wiki/concepts/`、`wiki/entities/`、`wiki/sources/` 下每个页面都必须有 frontmatter。索引与主题地图按它分组，`scripts/kb_meta.py check` 会校验（也集成进 `lint`）。

```yaml
# 概念页
type: concept
topic: GPU 编程     # 见下方固定词表
sources: 3          # 派生字段，勿手填
updated: 2026-07-12 # 派生字段，勿手填
```

实体页多一个 `entity_type`，来源页多一个 `source_kind` 且没有 `sources`。

固定词表（新增取值前先想清楚是否真的是新主题，词表膨胀等于没有分类）：

- `topic`：`注意力机制` `KV Cache` `推理服务` `并行与分布式` `GPU 编程` `性能分析` `模型架构` `投机解码` `训练与 Scaling` `位置编码`
- `entity_type`：`项目` `框架` `模型` `公司` `组织` `人物` `课程` `硬件` `benchmark`
- `source_kind`：`文章` `论文` `课程` `代码` `面试整理` `截图整理` `repo` `数据集`

`sources` 与 `updated` 由 `python3 scripts/kb_meta.py sync` 从实际链接数和 git 提交日期刷新，不要手写。词表本身定义在 `scripts/kb_meta.py`，改词表要同时改那里。

## 核心操作

三个核心动作的**执行流程**写在 `.claude/skills/` 下（`kb-ingest` / `kb-query` / `kb-export`），本文件只定义**规则约束**。两者分工：skill 说“按什么顺序做”，schema 说“什么算写对了”。改规则改这里，改流程改 skill。

### Ingest

输入：一份或一批新的原始资料。

流程：

1. 阅读完整原始资料
2. 先向用户汇报 `2-3` 条核心摘要和值得关注的论断
3. 等用户确认后，再创建或更新 `wiki/sources/`
4. 更新相关 `wiki/entities/` 与 `wiki/concepts/`
5. 检查新来源与已有 wiki 内容的矛盾，用显式标注记录
6. 更新所有受影响页面的交叉引用
7. 更新 `wiki/index.md`
8. 追加到 `wiki/log.md`
9. 最后向用户汇报：创建了什么、更新了什么、有没有矛盾

### Query

输入：一个研究问题、架构比较请求、性能归因问题，或实现路线评估请求。

流程：

1. 先读 `wiki/index.md`
2. 按需读取相关概念页、来源页、实体页
3. 输出为 Markdown 报告、对比表、Marp 幻灯片或可视化
4. 对高价值结论进行回填
5. 记录到 `wiki/log.md`

### Export

输入：一个需要反复记忆的主题。

流程：

1. 从概念页、实体页和 `output/interview/` 的面试备考资料里选料
2. 写成 `output/cards/<主题>.md` 的问答卡片，回链来源页面
3. 用 `scripts/export_cards.py` 转成 Anki 导入文件
4. 记录到 `wiki/log.md`

约束：

- 标了“待核实”的结论、没有一手来源的 benchmark 数字不出卡
- 一张卡一个知识点，且必须独立成立（Anki 会打乱顺序）
- 卡片是 wiki 的派生物，不是新的事实来源；发现卡片写错，回去改 wiki 页面再重新导出

### Lint

输入：对 wiki 做健康检查。

重点检查：

- 断链
- 孤立页面
- 来源页缺少原始资料引用
- 被频繁提及但尚无独立页面的概念或实体
- 过时表述、冲突表述、重复页面
- 可以进一步研究的问题

## 写作标准

- AI infra 表达优先准确、克制、可追溯
- 区分“原文说法”“实现细节”“实验观察”“LLM 归纳”
- 遇到结论不确定时，明确写“待核实”
- 避免伪造 benchmark 数字、吞吐数据、硬件配置或版本信息
- 不要把一次性问答直接塞进概念页，先整理成结构化内容

## 页面建议结构

- 来源页：来源信息、2-3 条摘要、值得关注的论断、关联概念、关联实体、待确认点
- 概念页：定义、核心机制、权衡、相关实体、相关来源
- 实体页：一句话说明、类型、核心信息、相关概念、相关来源、冲突与备注

## 编辑原则

- 优先对现有页面做局部更新，不要全文重写
- 单个来源可能触发多个页面更新，但每次编辑都应最小化改动范围
- 更新前先读目标页面，理解已有结构后再补充
- 如果只是新增一个小节，就只新增该小节，不重写整页

## Obsidian 约定

- 优先使用 `[[Wiki Links]]` 链接 Vault 内页面
- Vault 内引用图片时，优先使用 `![[raw/images/文件名.png]]`
- 目录导航页、首页、索引页要尽量可浏览、可点击、可回溯
- 不要输出只适合终端阅读而不适合 Obsidian 长期保存的格式
